function recebeNumero (n1:number, n2:number):number{
    const somar = (primeiroNumero + segundoNumero)
    const multiplicar = (primeiroNumero * segundoNumero)
    const subtracao = (primeiroNumero / segundoNumero)

    
    // const primeiroNumero:number = Number(process.argv[2]);
    // const segundoNumero:number =  Number(process.argv[3]);
    // const segundoNumero:number =  Number(process.argv[4]);
    return(
        

    )

}

console.log (recebeNumero(primeiroNumero + segundoNumero))
//dar um console log na soma deles, outro console.log na subtração, outro na multiplicação e outro no maior numero, no final de tudo vai chamar a função passando os parametros(os dois numeros)

