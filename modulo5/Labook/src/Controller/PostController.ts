

//APENAS AQUI TEM REQ. E RES. 

import { Request, Response } from "express";

export class PostController{

    post = (req:Request, res:Response) =>{
        const { photo, description, creationDate, type } = req.body

        const token = 

    }
}